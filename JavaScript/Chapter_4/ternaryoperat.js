let age = 18;

age < 18 ? console.log("Denied") : console.log("Allowed");

let score = 72;
let grade = score >= 80 ? "A" : score >= 70 ? "B" : score >= 60 ? "C" : "D";

console.log(`Your Exam Grade: ${grade}`);